% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rashomon.R, R/rashomon_analysis.R
\name{get_tree_rules}
\alias{get_tree_rules}
\title{Get Human-Readable Rules from Tree}
\usage{
get_tree_rules(tree_json, feature_names = NULL)

get_tree_rules(tree_json, feature_names = NULL)
}
\arguments{
\item{tree_json}{Character: JSON string representation of a tree}

\item{feature_names}{Character vector: names of features (optional)}

\item{tree}{A TreeClassifier object}
}
\value{
Character string with tree rules

A list containing:
  \item{rules}{List of decision rules}
  \item{leaves}{List of leaf node predictions}
  \item{features_used}{Vector of features used in the tree}
}
\description{
Convert a tree to human-readable if-then rules.

Extract human-readable rules from a tree's JSON representation.
}
\examples{
\dontrun{
trees <- get_rashomon_trees(model)
rules <- get_tree_rules(trees[[1]], feature_names = colnames(X))
cat(rules)
}

\dontrun{
# Get rules from a tree
rules <- get_tree_rules(tree_json, feature_names = colnames(X))
print(rules)
}

}
