% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treefarms.R
\name{get_probabilities}
\alias{get_probabilities}
\title{Predict using a trained TreeFARMS model}
\usage{
get_probabilities(object)
}
\arguments{
\item{object}{A treefarms_model object}

\item{newdata}{A data.frame or matrix of new features to predict on.}

\item{type}{Character string specifying the type of prediction.
Options: "class" (binary predictions) or "prob" (probabilities). Default: "class".}

\item{...}{Additional arguments (currently unused).}
}
\value{
For \code{type = "class"}: A vector of binary predictions (0/1).
  For \code{type = "prob"}: A matrix with columns [P(class=0), P(class=1)].

A matrix of probabilities [P(class=0), P(class=1)]
}
\description{
Predict using a trained TreeFARMS model
}
\examples{
\dontrun{
# Train a model
model <- treefarms(X, y, loss_function = "log_loss")

# Get binary predictions
pred_class <- predict_treefarms(model, X_new, type = "class")

# Get probability predictions
pred_prob <- predict_treefarms(model, X_new, type = "prob")
}

Get probabilities from a treefarms model (with lazy evaluation)

}
