% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom_loss.R
\name{validate_custom_loss}
\alias{validate_custom_loss}
\title{Validate Custom Loss Function Specification}
\usage{
validate_custom_loss(custom_loss)
}
\arguments{
\item{custom_loss}{A list with required components:
\itemize{
  \item \code{aggregate}: Function that takes a numeric vector and returns
    a named list of summary statistics
  \item \code{loss}: Function that takes summary statistics and a prediction,
    returns a list with \code{min_loss}, \code{max_loss}, and \code{potential}
  \item \code{optimal}: Function that takes summary statistics and returns
    the optimal prediction (single numeric value)
  \item \code{is_regression}: Logical flag indicating if this is a regression
    loss (TRUE) or classification loss (FALSE)
}}
}
\value{
TRUE if valid (invisibly), throws an error otherwise
}
\description{
Validates that a custom loss function specification contains all required
components and that each component has the correct type.
}
\examples{
\dontrun{
# Valid custom loss (squared error)
custom_se <- list(
  aggregate = function(y) {
    list(n = length(y), mean = mean(y), sum_sq = sum(y^2))
  },
  loss = function(stats, pred) {
    sse <- stats$sum_sq - 2*pred*stats$mean*stats$n + pred^2*stats$n
    list(min_loss = sse, max_loss = sse, potential = 0)
  },
  optimal = function(stats) stats$mean,
  is_regression = TRUE
)
validate_custom_loss(custom_se)
}

}
