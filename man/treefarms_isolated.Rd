% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treefarms_isolated.R
\name{treefarms_isolated}
\alias{treefarms_isolated}
\title{TreeFARMS with Process Isolation}
\usage{
treefarms_isolated(
  X,
  y,
  loss_function = "misclassification",
  regularization = 0.1,
  rashomon_bound_multiplier = 0.05,
  worker_limit = 1,
  verbose = FALSE,
  single_tree = TRUE,
  timeout = 3600,
  store_training_data = FALSE,
  compute_probabilities = FALSE,
  ...
)
}
\arguments{
\item{X}{A data.frame or matrix of features. Must contain only binary (0/1) features.}

\item{y}{A vector of binary class labels (0/1).}

\item{loss_function}{Character string: "misclassification" or "log_loss" (default: "misclassification").}

\item{regularization}{Numeric value controlling model complexity (default: 0.1).}

\item{rashomon_bound_multiplier}{Numeric value controlling Rashomon set size (default: 0.05).}

\item{worker_limit}{Integer: number of parallel workers (default: 1).}

\item{verbose}{Logical: print training progress (default: FALSE).}

\item{single_tree}{Logical: if TRUE, fit exactly one tree (default: TRUE).}

\item{timeout}{Numeric: timeout in seconds for subprocess (default: 3600, 1 hour).}

\item{store_training_data}{Logical: whether to store training data in model (default: FALSE).}

\item{compute_probabilities}{Logical: whether to compute probabilities immediately (default: FALSE).}

\item{...}{Additional parameters passed to TreeFARMS configuration.}
}
\value{
Same as \code{\link{treefarms}}: list with model, predictions, probabilities, etc.
}
\description{
Wrapper for treefarms that uses process isolation to prevent C++ crashes
from affecting the R session. This is the recommended approach for
production use, especially when dealing with large datasets or complex models.
}
\details{
This function provides process isolation by:
1. Writing data and configuration to temporary files
2. Calling the standalone gosdt executable in a separate process
3. Reading results from output file
4. Cleaning up temporary files

If the executable is not found, falls back to direct Rcpp call with a warning.
}
