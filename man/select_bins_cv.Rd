% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discretize.R
\name{select_bins_cv}
\alias{select_bins_cv}
\title{Select the number of discretization bins by cross-validation}
\usage{
select_bins_cv(
  X,
  y,
  loss_function = "log_loss",
  candidate_bins = NULL,
  K = 5L,
  regularization = NULL,
  method = "quantiles"
)
}
\arguments{
\item{X}{Data frame of features (continuous and/or binary).}

\item{y}{Outcome vector.}

\item{loss_function}{"misclassification", "log_loss", or "squared_error".}

\item{candidate_bins}{Integer vector of candidate bin counts to try. If NULL,
a default geometric-ish grid spanning the log and polynomial schedules is used.}

\item{K}{Number of CV folds (default 5).}

\item{regularization}{Regularization passed to \code{fit_tree} during CV
(default \code{(log n)/n}, the theory rate on the mean-loss scale).}

\item{method}{Discretization method ("quantiles" recommended for CV).}
}
\value{
A list with \code{best_bins} (integer), \code{cv_loss} (numeric vector
  aligned with \code{candidate_bins}), and \code{candidate_bins}.
}
\description{
Data-driven alternative to the fixed \code{"adaptive"} polynomial schedule
(\code{\link{compute_bin_count}}). Chooses the per-coordinate bin count from a
candidate grid by minimizing \eqn{K}-fold held-out risk under the given loss.
Because the theory-optimal grid resolution depends on the unknown smoothness
and sparsity of the target, cross-validation is the practical way to adapt the
grid to the data; the polynomial default is the theory-motivated fallback.
}
\keyword{internal}
