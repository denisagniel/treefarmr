% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epsilon_selection.R
\name{extract_best_score}
\alias{extract_best_score}
\title{Helper: Extract best penalized risk from TreeFARMS models}
\usage{
extract_best_score(model)
}
\arguments{
\item{model}{A fitted TreeFARMS model (from \code{\link{fit_tree}} or
\code{\link{treefarms}}).}
}
\value{
Numeric. The minimum penalized empirical risk achieved by the model.
}
\description{
Extracts the best (minimum) penalized risk value from a fitted TreeFARMS model.
Useful for computing \code{best_scores} for \code{\link{select_epsilon_n}} with
\code{method = "adaptive"}.
}
\details{
For a model fitted with regularization \eqn{\lambda} and loss function \eqn{L},
returns:
\deqn{\text{best} = \min_{f \in \text{trees}} \Bigl[ \frac{1}{n} \sum_{i=1}^n L(y_i, f(x_i)) + \lambda \cdot (\#\text{leaves of } f) \Bigr]}

This is the value that defines the Rashomon set for that fold.
}
\examples{
\dontrun{
# Fit model on fold k
model_k <- fit_tree(X, y, regularization = 0.1)

# Extract best penalized risk
best_k <- extract_best_score(model_k)
}

}
\keyword{internal}
