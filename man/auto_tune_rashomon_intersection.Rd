% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_tune_rashomon.R
\name{auto_tune_rashomon_intersection}
\alias{auto_tune_rashomon_intersection}
\title{Auto-Tune Rashomon Bound Multiplier for Non-Empty Intersection}
\usage{
auto_tune_rashomon_intersection(
  X,
  y,
  K,
  fold_indices,
  loss_function,
  regularization,
  c_start = 1,
  c_max = 100,
  binary_tolerance = 0.1,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{X}{Feature matrix (already discretized)}

\item{y}{Outcome vector}

\item{K}{Number of folds}

\item{fold_indices}{Fold assignment vector}

\item{loss_function}{Loss function ("log_loss", "misclassification", "squared_error")}

\item{regularization}{Regularization parameter (fixed)}

\item{c_start}{Starting constant (default: 1)}

\item{c_max}{Maximum constant (default: 100)}

\item{binary_tolerance}{Tolerance for binary refinement (default: 0.1)}

\item{verbose}{Logical, print diagnostic info}

\item{...}{Additional arguments passed to try_cross_fitted_rashomon_internal()}
}
\value{
List with:
  - epsilon_n: Selected epsilon_n value
  - n_intersecting: Number of intersecting trees
  - converged: TRUE if intersection found, FALSE if all attempts failed
  - attempts: Number of attempts tried
  - result: Full cross_fitted_rashomon result (or NULL if failed)
}
\description{
Uses exponential search followed by binary refinement to find the smallest
epsilon_n that yields non-empty intersection across K folds. Then selects
the best tree from the intersection by lowest penalized risk.

Algorithm:
1. Exponential phase: Try c = 1, 2, 4, 8, 16, ... (doubling)
2. Stop when first intersection found
3. Binary refinement: Search between last-fail and first-success
4. Tree selection: Pick tree with lowest R_pen from final intersection
}
\keyword{internal}
