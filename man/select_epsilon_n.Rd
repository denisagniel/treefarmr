% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epsilon_selection.R
\name{select_epsilon_n}
\alias{select_epsilon_n}
\title{Select Rashomon Tolerance Parameter (epsilon_n)}
\usage{
select_epsilon_n(
  n,
  method = c("fixed", "adaptive", "grid"),
  c = 2,
  best_scores = NULL,
  grid_constants = c(1, 2, 3, 5, 10)
)
}
\arguments{
\item{n}{Integer. Sample size.}

\item{method}{Character. Selection method:
\itemize{
  \item \code{"fixed"} (default): \eqn{\varepsilon_n = c \sqrt{\log(n)/n}}
  \item \code{"adaptive"}: Scale by cross-fold coefficient of variation
  \item \code{"grid"}: Return grid of values for grid search
}}

\item{c}{Numeric. Constant multiplier for fixed method. Default: 2.
Typical values: 1 (tight), 2 (moderate), 3 (loose).}

\item{best_scores}{Numeric vector. Fold-specific best penalized risk values,
required for \code{method = "adaptive"}. Should be \code{best^{(-k)}} from
each fold \eqn{k = 1, \ldots, K}.}

\item{grid_constants}{Numeric vector. Constants for grid method.
Default: \code{c(1, 2, 3, 5, 10)}.}
}
\value{
\itemize{
    \item For \code{method = "fixed"} or \code{"adaptive"}: Single numeric value
    \item For \code{method = "grid"}: Numeric vector of \eqn{\varepsilon_n} values
  }
}
\description{
Provides theoretically justified choices for the Rashomon set tolerance parameter
\eqn{\varepsilon_n}, following the manuscript guidance (Remark: Practical choice
of \eqn{\varepsilon_n}). All methods ensure \eqn{\varepsilon_n = o(n^{-1/2})}
for asymptotic validity in DML applications.
}
\details{
\strong{Three theoretically justified approaches:}

\strong{1. Fixed default (\code{method = "fixed"}):}

\deqn{\varepsilon_n = c \sqrt{\frac{\log n}{n}}}

Simple, deterministic, satisfies \eqn{o(n^{-1/2})}. Conservative relative to
empirical process fluctuation scale. Recommended for standard use.

\strong{2. Adaptive scaling (\code{method = "adaptive"}):}

\deqn{\varepsilon_n = \sqrt{\frac{\log n}{n}} \cdot \max\Bigl(1, \frac{\text{sd}(\text{best}^{(-k)})}{\text{median}(\text{best}^{(-k)})}\Bigr)}

Adapts to cross-fold variability: tighter Rashomon sets when folds are similar,
looser when folds are variable. Preserves \eqn{\varepsilon_n = o_p(n^{-1/2})}
since the coefficient of variation is \eqn{O_p(1)}.

\strong{3. Grid search (\code{method = "grid"}):}

Returns \eqn{\{c_1 \sqrt{\log n/n}, \ldots, c_M \sqrt{\log n/n}\}} for
\code{grid_constants} \eqn{= (c_1, \ldots, c_M)}. Use with
\code{\link{cross_fitted_rashomon}} auto-tuning: select smallest value yielding
non-empty intersection. Asymptotically valid since all grid points are
\eqn{o(n^{-1/2})} by construction.

\strong{Interpretation:}

\eqn{\varepsilon_n} controls the Rashomon set via
\deqn{R_k(\varepsilon) = \bigl\{ f : \text{penalized risk}(f) \le (1 + \varepsilon) \cdot \text{best}^{(-k)} \bigr\}.}

Smaller \eqn{\varepsilon_n} yields tighter sets (trees closer to optimal) but
increases risk of empty intersection across folds. Larger \eqn{\varepsilon_n}
facilitates intersection but includes more sub-optimal trees. The bias penalty
for using an intersection tree is \eqn{O_p(\varepsilon_n)}.
}
\examples{
# Fixed default (recommended starting point)
n <- 500
epsilon_n <- select_epsilon_n(n, method = "fixed", c = 2)
epsilon_n

# Tighter tolerance
epsilon_tight <- select_epsilon_n(n, method = "fixed", c = 1)

# Adaptive (requires fold-specific best scores)
# Example: K=5 folds, best^{(-k)} values from each fold
best_scores <- c(0.245, 0.268, 0.239, 0.261, 0.277)
epsilon_adaptive <- select_epsilon_n(n, method = "adaptive",
                                     best_scores = best_scores)
epsilon_adaptive

# Grid for auto-tuning
epsilon_grid <- select_epsilon_n(n, method = "grid")
epsilon_grid
# Use with: cross_fitted_rashomon(..., rashomon_bound_multiplier = epsilon_grid[1])
# and increase if intersection empty

}
\references{
See manuscript Appendix A.5, Remark: "Practical choice of \eqn{\varepsilon_n}".
}
