% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rashomon.R
\name{tree_to_partition_signature}
\alias{tree_to_partition_signature}
\title{Extract partition signature from tree (for binary features)}
\usage{
tree_to_partition_signature(tree, n_features = NULL)
}
\arguments{
\item{tree}{A tree as list (with \code{feature}/\code{prediction}) or JSON string}

\item{n_features}{Number of binary features (default: detect from tree)}
}
\value{
A sorted character vector mapping inputs to leaf IDs
}
\description{
For trees with binary features, extract which inputs go to which leaf IDs.
Two trees with different split orders but the same partition structure
(same grouping of inputs into leaves) will have equivalent signatures.

This captures the STRUCTURE of the partition, not the leaf values, which
is appropriate for DML where we want to use the same partition across folds
but fit different leaf values per fold.
}
