% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_optimality_certificate.R
\name{tree_leaf_boxes}
\alias{tree_leaf_boxes}
\title{Per-leaf axis-aligned box constraints of a coordinate-space tree}
\usage{
tree_leaf_boxes(tree, coord_names)
}
\arguments{
\item{tree}{A coordinate-space tree (see \code{\link{stage_b}}).}

\item{coord_names}{Character vector of coordinates to track bounds for.}
}
\value{
Data.frame with one row per (leaf, constrained coordinate):
  \code{leaf_id}, \code{coord}, \code{lo}, \code{hi}.
}
\description{
Root-to-leaf walk maintaining, per coordinate, an open-below/closed-above
interval \code{(lo, hi]} (matching the tree's own left := \code{X[[coord]]
<= cut} convention). Coordinates never split on, on the path to a given
leaf, keep their trivial \code{(-Inf, Inf)} bound and are OMITTED from
that leaf's rows -- there is nothing to constrain and including a
trivial row would only add noise to any downstream comparison.
}
\keyword{internal}
